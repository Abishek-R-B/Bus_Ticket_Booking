# 🚌 Bus Ticket Booking Platform

A full-stack bus ticket booking application built with **React**, **Express.js**, and **Node.js**.  
Users can register, log in, and book bus tickets easily. Admins can manage (CRUD) bus details, routes, and bookings.

---

## 🚀 Getting Started

### 🔧 Backend Setup
1. Navigate to the backend folder:
   ```bash
   cd back-end
````

2. Install dependencies:

   ```bash
   npm install
   ```
3. Start the development server:

   ```bash
   npm run dev
   ```

   The backend server will start running (usually on port **5002** or as defined in your `.env`).

---

### 💻 Frontend Setup

1. Navigate to the frontend folder:

   ```bash
   cd front-end
   ```
2. Install dependencies:

   ```bash
   npm install
   ```
3. Start the frontend:

   ```bash
   npm run dev
   ```

   The frontend will run (usually on **[http://localhost:5173](http://localhost:5173)** or similar).

---

## 🔐 Admin Access

Use the following credentials to log in as **Admin**:

```
Email: Admin@admin.com  
Password: Admin@123
```

✅ **Admins** can:

* Create, Read, Update, and Delete buses, routes, and schedules
* Manage users and bookings

---

## 👥 User Access

If you’re not an admin, you can **register** a new account and use the platform to:

* Search for buses
* Select and book seats (up to 6 seats per booking)
* View your ticket history

---

## 🧩 Tech Stack

* **Frontend:** React, Tailwind CSS, Axios
* **Backend:** Node.js, Express.js
* **Database:** PostgreSQL 
* **Authentication:** JWT-based login

---

## 🛠️ Scripts Summary

| Command         | Description                      |
| --------------- | -------------------------------- |
| `npm install`   | Installs dependencies            |
| `npm run dev`   | Runs the app in development mode |
| `npm run build` | Builds for production            |

---

### 💡 Tip

Make sure your **backend server** is running before launching the frontend for a smooth experience.

---

**Developed with ❤️ using React & Express.js**

```
